{
  "name": "dace-webclient",
  "version": "1.0.0",
  "description": "This repository hosts the JavaScript side for web and VSCode clients of DaCe and the DIODE.",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
 


 

  "dependencies": {
    "@types/dagre": "^0.7.44"
  }
}
